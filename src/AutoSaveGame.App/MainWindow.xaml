<Window x:Class="AutoSaveGame.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AutoSaveGame.App.ViewModels"
        Title="AutoSaveGame"
        Icon="Assets/AutoSaveGame.ico"
        Width="360"
        Height="480"
        MinWidth="360"
        MinHeight="480"
        MaxWidth="360"
        MaxHeight="480"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True"
        Background="{StaticResource WindowBackground}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated"
        Closing="Window_Closing"
        KeyboardNavigation.TabNavigation="Continue">
    <Window.Resources>
        <Style x:Key="CompactButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="SignInCircleButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="64" />
            <Setter Property="Height" Value="64" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="32">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.85" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SignInRing" TargetType="Path">
            <Setter Property="Width" Value="96" />
            <Setter Property="Height" Value="96" />
            <Setter Property="Stroke" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="StrokeThickness" Value="4" />
            <Setter Property="StrokeStartLineCap" Value="Round" />
            <Setter Property="StrokeEndLineCap" Value="Round" />
            <Setter Property="Visibility" Value="Collapsed" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform CenterX="48" CenterY="48" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSigningIn}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                 From="0"
                                                 To="360"
                                                 Duration="0:0:1.2"
                                                 RepeatBehavior="Forever" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard />
                    </DataTrigger.ExitActions>
                </DataTrigger>
                <DataTrigger Binding="{Binding HasSignInError}" Value="True">
                    <Setter Property="Stroke" Value="{StaticResource DangerBrush}" />
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Background="{StaticResource WindowBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64" />
                <RowDefinition Height="4" />
                <RowDefinition Height="*" />
                <RowDefinition Height="46" />
            </Grid.RowDefinitions>

            <Border Padding="16,12"
                    Background="White"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="34" />
                    </Grid.ColumnDefinitions>
                    <Border Width="34" Height="34"
                            Background="{StaticResource SoftBlueBrush}"
                            CornerRadius="10">
                        <TextBlock Text="☁"
                                   FontSize="20"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="AutoSaveGame"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextStrongBrush}" />
                        <TextBlock FontSize="11" Foreground="{StaticResource TextMutedBrush}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Chưa kết nối Google Drive" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                            <Setter Property="Text" Value="Google Drive đã kết nối" />
                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <Button Grid.Column="2"
                            Style="{StaticResource CompactButton}"
                            Content="×"
                            FontSize="18"
                            Padding="0"
                            ToolTip="Ẩn cửa sổ"
                            Click="Hide_Click" />
                </Grid>
            </Border>

            <ProgressBar Grid.Row="1"
                         BorderThickness="0"
                         Foreground="{StaticResource PrimaryBrush}"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Value="{Binding OperationPercent, Mode=OneWay}"
                         IsTabStop="False"
                         AutomationProperties.Name="Tiến độ thao tác"
                         Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <Grid Grid.Row="2" Margin="14,12">
                <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Giữ save game luôn bên bạn"
                               FontSize="23"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextStrongBrush}"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />
                    <TextBlock Margin="12,10,12,20"
                               Text="Đăng nhập một lần trong phiên này để khôi phục và tự động sao lưu thay đổi lên vùng riêng của ứng dụng."
                               Foreground="{StaticResource TextMutedBrush}"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               LineHeight="20" />
                    <StackPanel HorizontalAlignment="Center">
                        <Grid Width="96" Height="96">
                            <Path Style="{StaticResource SignInRing}"
                                  Data="M 48,4 A 44,44 0 1 1 9.9,70" />
                            <Button Style="{StaticResource SignInCircleButton}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Command="{Binding SignInCommand}"
                                    AutomationProperties.Name="Đăng nhập bằng Google">
                                <TextBlock Text="G"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="White" />
                            </Button>
                        </Grid>
                        <TextBlock HorizontalAlignment="Center"
                                   Margin="0,10,0,0"
                                   Text="Đăng nhập bằng Google"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextStrongBrush}" />
                        <TextBlock HorizontalAlignment="Center"
                                   Text="{Binding SignInHint}"
                                   FontSize="11"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    <Border Margin="0,18,0,0" Padding="12"
                            Background="{StaticResource SoftBlueBrush}"
                            CornerRadius="10">
                        <TextBlock Text="Token chỉ lưu trong RAM và bị xóa khi thoát. Dữ liệu save nằm trong appDataFolder riêng của ứng dụng."
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   LineHeight="17" />
                    </Border>
                </StackPanel>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOverviewVisible}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Game đã bảo vệ"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextStrongBrush}" />
                            <TextBlock Text="Mỗi game giữ một snapshot đã xác minh"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}" />
                        </StackPanel>
                        <Button HorizontalAlignment="Right"
                                Style="{StaticResource CompactButton}"
                                Content="+ Thêm game"
                                Click="AddGame_Click" />
                    </Grid>

                    <ScrollViewer Grid.Row="1" Margin="0,10,0,8"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding Games}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:GameItemViewModel}">
                                    <Button Style="{StaticResource GlassGameCard}"
                                            Command="{Binding DataContext.SelectGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}">
                                        <StackPanel>
                                            <Grid>
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontWeight="SemiBold"
                                                           FontSize="15"
                                                           Foreground="{StaticResource TextStrongBrush}" />
                                                <Border HorizontalAlignment="Right"
                                                        Padding="7,2"
                                                        Background="{StaticResource SoftBlueBrush}"
                                                        CornerRadius="8">
                                                    <TextBlock Text="{Binding StatusText}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource PrimaryBrush}" />
                                                </Border>
                                            </Grid>
                                            <TextBlock Margin="0,6,0,0"
                                                       Text="{Binding LocalPath}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding LocalPath}" />
                                            <Grid Margin="0,8,0,0">
                                                <TextBlock Text="{Binding LastBackupDisplayText}"
                                                           FontSize="10"
                                                           Foreground="{StaticResource TextMutedBrush}" />
                                                <TextBlock HorizontalAlignment="Right"
                                                           Text="{Binding ArchiveSizeText}"
                                                           FontSize="10"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextStrongBrush}" />
                                            </Grid>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Expander Grid.Row="2" Header="Dữ liệu ứng dụng trên Drive"
                              FontSize="11" Foreground="{StaticResource TextMutedBrush}">
                        <Border Margin="0,6,0,0" Padding="10"
                                Background="{StaticResource MutedSurfaceBrush}"
                                CornerRadius="8">
                            <StackPanel>
                                <TextBlock Text="{Binding CloudUsageText}"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextStrongBrush}" />
                                <TextBlock Margin="0,3,0,0"
                                           Text="appDataFolder là vùng riêng, không hiện trong danh sách file Google Drive thông thường."
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Expander>
                </Grid>

                <Grid DataContext="{Binding SelectedGame}">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.IsGameDetailVisible, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Button Content="← Quay lại"
                                Style="{StaticResource CompactButton}"
                                Command="{Binding DataContext.BackToOverviewCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                        <TextBlock HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Text="Chi tiết game"
                                   FontSize="12"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </Grid>
                    <ScrollViewer Grid.Row="1" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextStrongBrush}" />
                            <TextBlock Margin="0,4,0,10"
                                       Text="{Binding StatusText}"
                                       Foreground="{StaticResource PrimaryBrush}" />
                            <Border Style="{StaticResource Card}" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Thư mục save" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding LocalPath}" TextWrapping="Wrap" FontSize="11" />
                                    <TextBlock Margin="0,8,0,0" Text="Bản sao Drive" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding LastBackupDisplayText}" FontSize="11" />
                                    <TextBlock Text="{Binding ArchiveSizeText}" FontSize="11" />
                                    <TextBlock Text="{Binding ArchiveFileIdDisplay}" TextWrapping="Wrap" FontSize="10" />
                                    <TextBlock Text="{Binding ArchiveSha256Display}" TextWrapping="Wrap" FontSize="10" />
                                </StackPanel>
                            </Border>
                            <WrapPanel Margin="0,0,0,10">
                                <Button Margin="0,0,5,5" Style="{StaticResource CompactButton}"
                                        Content="Sao lưu"
                                        Command="{Binding DataContext.BackupNowCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                                <Button Margin="0,0,5,5" Style="{StaticResource CompactButton}"
                                        Content="Khôi phục"
                                        Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                            </WrapPanel>
                            <Border Padding="10"
                                    Background="{StaticResource DangerSurfaceBrush}"
                                    BorderBrush="{StaticResource DangerBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="Dữ liệu appDataFolder ẩn của Google Drive"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource DangerBrush}" />
                                    <TextBlock Margin="0,4,0,8"
                                               Text="Xóa bản sao lưu Drive của game này. File save trên máy không bị xóa."
                                               FontSize="11"
                                               TextWrapping="Wrap" />
                                    <Button Content="Xóa dữ liệu Drive của game này"
                                            Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.DeleteCloudDataCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <Border Grid.Row="3" Padding="14,8"
                    Background="White"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center"
                               AutomationProperties.LiveSetting="Polite" />
                    <Button HorizontalAlignment="Right"
                            Content="Đăng xuất"
                            Command="{Binding SignOutCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CompactButton}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
