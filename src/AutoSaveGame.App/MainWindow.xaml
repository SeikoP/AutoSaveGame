<Window x:Class="AutoSaveGame.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AutoSaveGame.App.ViewModels"
        Title="AutoSaveGame"
        Width="920"
        Height="620"
        MinWidth="760"
        MinHeight="480"
        Background="{StaticResource WindowBackground}"
        WindowStartupLocation="CenterScreen"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel>
                <TextBlock Text="AutoSaveGame"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#111827" />
                <TextBlock Margin="0,4,0,0"
                           Text="Keep one verified game-save snapshot in your Google Drive."
                           Foreground="#6B7280" />
            </StackPanel>
            <Button HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Content="Exit"
                    Click="Exit_Click" />
        </Grid>

        <Border Grid.Row="1"
                Margin="0,24,0,18"
                Padding="24"
                Background="{StaticResource CardBackground}"
                CornerRadius="12">
            <Grid>
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            MaxWidth="440">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Restore your saves on this computer"
                               FontSize="22"
                               FontWeight="SemiBold"
                               TextAlignment="Center" />
                    <TextBlock Margin="0,10,0,22"
                               Text="Sign in every session. AutoSaveGame never stores your Google refresh token on this public computer."
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Foreground="#6B7280" />
                    <Button Padding="22,11"
                            HorizontalAlignment="Center"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="White"
                            Content="Sign in with Google"
                            Command="{Binding SignInCommand}" />
                </StackPanel>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSignedIn}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <TextBlock Text="Your games"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <Button HorizontalAlignment="Right"
                                Content="+ Add game"
                                Click="AddGame_Click" />
                    </Grid>
                    <ListBox Grid.Row="1"
                             Margin="0,16,0,0"
                             BorderThickness="0"
                             Background="Transparent"
                             ItemsSource="{Binding Games}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0,0,0,10" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:GameItemViewModel}">
                                <Border Padding="16"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1"
                                        CornerRadius="9">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="17"
                                                           FontWeight="SemiBold" />
                                                <Border Margin="10,0,0,0"
                                                        Padding="7,2"
                                                        Background="#EFF6FF"
                                                        CornerRadius="8">
                                                    <TextBlock Text="{Binding Status}"
                                                               Foreground="#1D4ED8"
                                                               FontSize="12" />
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Margin="0,6,0,0"
                                                       Text="{Binding LocalPath}"
                                                       Foreground="#6B7280"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Margin="0,4,0,0"
                                                       Text="{Binding LastBackupText, StringFormat=Last cloud backup: {0}}"
                                                       Foreground="#6B7280"
                                                       FontSize="12" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1"
                                                    Orientation="Horizontal"
                                                    VerticalAlignment="Center">
                                            <CheckBox Margin="0,0,12,0"
                                                      VerticalAlignment="Center"
                                                      Content="Watch"
                                                      IsChecked="{Binding WatchEnabled, Mode=OneWay}"
                                                      Click="Watch_Click" />
                                            <Button Margin="0,0,7,0"
                                                    Content="Restore"
                                                    Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" />
                                            <Button Margin="0,0,7,0"
                                                    Content="Backup now"
                                                    Command="{Binding DataContext.BackupNowCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" />
                                            <Button Margin="0,0,7,0"
                                                    Content="Edit"
                                                    Click="EditGame_Click" />
                                            <Button Content="Remove"
                                                    Command="{Binding DataContext.DeleteGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="#4B5563"
                       VerticalAlignment="Center" />
            <TextBlock HorizontalAlignment="Right"
                       Text="Only a confirmed cloud backup is safe."
                       Foreground="#9CA3AF"
                       VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
